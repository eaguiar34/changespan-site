import PortalShell from "@/components/site/portal-shell";
import { getRevisionCompare } from "@/lib/api";
export default async function PortalRevisionComparePage() {
  const data = await getRevisionCompare('default');
  return <PortalShell title="Revision Compare" description="Compare active package scope against the prior published revision."><div className="grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-xl font-semibold">Summary</h2><ul className="mt-4 space-y-2 text-sm text-slate-700">{data.summary.map((item: string) => <li key={item}>• {item}</li>)}</ul></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-xl font-semibold">Metrics</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><div className="rounded-xl bg-slate-50 p-4"><div className="text-slate-500 text-sm">Schedule delta</div><div className="mt-1 text-2xl font-black">{data.metrics.schedule_delta_days} days</div></div><div className="rounded-xl bg-slate-50 p-4"><div className="text-slate-500 text-sm">Cost delta</div><div className="mt-1 text-2xl font-black">${data.metrics.cost_delta.toLocaleString()}</div></div><div className="rounded-xl bg-slate-50 p-4"><div className="text-slate-500 text-sm">Open comments</div><div className="mt-1 text-2xl font-black">{data.metrics.open_comments}</div></div><div className="rounded-xl bg-slate-50 p-4"><div className="text-slate-500 text-sm">Attachment change</div><div className="mt-1 text-2xl font-black">{data.metrics.attachment_delta}</div></div></div></section></div></PortalShell>;
}
